name: hello-world
on: push
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: my-step
        run: |
          echo "GITHUB_WORKSPACE : $GITHUB_WORKSPACE"
          echo "ITHUB_ENV: $GITHUB_ENV"
